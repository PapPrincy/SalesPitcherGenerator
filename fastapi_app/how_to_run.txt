"""Run the Application: 
    In your terminal, with your virtual environment activated (venv), 
run the following command to start your FastAPI application.
                  uvicorn main:app
Once uvicorn starts the server, open a web browser and go to 
http://127.0.0.1:8000 (or another address and port if you specified different ones). 
You should see your input form (input_form.html). Fill in a URL and submit the form to generate 
and display the sales pitch and email content on output.html."""